# Local standalone fallbacks only.
# In MissionSquad, users should configure these as hidden server secrets instead.
DROPBOX_ACCESS_TOKEN=
DROPBOX_EMAIL=

# Runtime Configuration
LOG_LEVEL=info
DROPBOX_RETRY_MAX_ATTEMPTS=3
DROPBOX_RETRY_BASE_DELAY_MS=250
