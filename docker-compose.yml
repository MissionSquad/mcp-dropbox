services:
  dbx-mcp-server:
    build:
      context: .
      dockerfile: Dockerfile
    stdin_open: true
    tty: true
    environment:
      LOG_LEVEL: "info"
      DROPBOX_RETRY_MAX_ATTEMPTS: "3"
      DROPBOX_RETRY_BASE_DELAY_MS: "250"
